import styled from "styled-components";

export const StyledInput = styled.input`
  width: 100%;
  font-size: 40px;
  font-weight: 700;
  color: rgb(55, 53, 47);
  outline: none;
  border: none;
  margin-bottom: 20px;
  padding: 0 96px;

  ::placeholder {
    opacity: 0.2;
  }
`;
