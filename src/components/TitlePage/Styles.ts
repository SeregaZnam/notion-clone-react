import styled from 'styled-components';

export const StyledInput = styled.input`
  font-size: 40px;
  font-weight: 700;
  color: rgb(55, 53, 47);
  outline: none;
  border: none;

  ::placeholder {
    opacity: 0.2;
  }
`;
