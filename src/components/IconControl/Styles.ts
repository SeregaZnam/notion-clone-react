import styled from "styled-components";

export const StyledIconControl = styled.div`
  display: flex;
  height: 28px;
  padding: 0 8px 0 8px;

  .control-content {
    display: flex;
    align-items: center;

    .control-text {
      margin-left: 5px;
    }
  }
`;
